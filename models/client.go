package models

// IMAGE_PATH is the path to the image file
// If this folder does not exist, it will be created
const IMAGE_PATH = "images"

// IMAGE_PATH_CHMOD is the permission to create the image path
const IMAGE_PATH_CHMOD = 0755
